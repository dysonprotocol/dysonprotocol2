diff --git a/cpython-unix/Makefile b/cpython-unix/Makefile
index 577f44f..3b3e992 100644
--- a/cpython-unix/Makefile
+++ b/cpython-unix/Makefile
@@ -79,7 +79,7 @@ $(OUTDIR)/image-%.$(HOST_PLATFORM).tar: $(OUTDIR)/%.Dockerfile
 	$(RUN_BUILD) --toolchain image-$*
 endif
 
-$(OUTDIR)/binutils-$(BINUTILS_VERSION)-$(HOST_PLATFORM).tar: $(OUTDIR)/image-$(DOCKER_IMAGE_GCC).$(HOST_PLATFORM).tar $(HERE)/build-binutils.sh
+$(OUTDIR)/binutils-$(BINUTILS_VERSION)-$(HOST_PLATFORM).tar: $(if $(PYBUILD_NO_DOCKER),,$(OUTDIR)/image-$(DOCKER_IMAGE_GCC).$(HOST_PLATFORM).tar) $(HERE)/build-binutils.sh
 	$(RUN_BUILD) --toolchain --docker-image $(DOCKER_IMAGE_GCC) binutils
 
 $(OUTDIR)/$(CLANG_FILENAME):
diff --git a/pythonbuild/buildenv.py b/pythonbuild/buildenv.py
index 4b2a0e6..dca7539 100644
--- a/pythonbuild/buildenv.py
+++ b/pythonbuild/buildenv.py
@@ -143,7 +143,12 @@ class TempdirContext(object):
     def __init__(self, td):
         self.td = pathlib.Path(td)
 
-        self.tools_path = str(self.td / "tools")
+        # When using /build (no-Docker Linux builds), tools must go to /tools
+        # to match shell scripts that hardcode paths like /tools/host/bin.
+        if str(self.td) == "/build":
+            self.tools_path = "/tools"
+        else:
+            self.tools_path = str(self.td / "tools")
 
     @property
     def is_isolated(self):
@@ -172,7 +177,7 @@ class TempdirContext(object):
         )
 
         p = build_dir / basename
-        dest_path = self.td / "tools"
+        dest_path = pathlib.Path(self.tools_path)
         log("extracting %s to %s" % (p, dest_path))
         extract_tar_to_directory(p, dest_path)
 
@@ -188,7 +193,7 @@ class TempdirContext(object):
         )
 
         p = build_dir / basename
-        dest_path = self.td / "tools"
+        dest_path = pathlib.Path(self.tools_path)
         log("extracting %s to %s" % (p, dest_path))
         extract_tar_to_directory(p, dest_path)
 
@@ -269,12 +274,18 @@ def build_environment(client, image):
         container = client.containers.run(
             image, command=["/bin/sleep", "86400"], detach=True
         )
-        td = None
         context = ContainerContext(container)
     else:
         container = None
-        td = tempfile.TemporaryDirectory()
-        context = TempdirContext(td.name)
+        # Use fixed paths matching shell script expectations (cd /build,
+        # DESTDIR=/build/out, --prefix=/tools/host, etc.) instead of a
+        # random temp directory.
+        for d in ("/build", "/tools"):
+            p = pathlib.Path(d)
+            if p.exists():
+                shutil.rmtree(p)
+            p.mkdir(parents=True)
+        context = TempdirContext("/build")
 
     try:
         yield context
@@ -282,5 +293,3 @@ def build_environment(client, image):
         if container:
             container.stop(timeout=0)
             container.remove()
-        else:
-            td.cleanup()

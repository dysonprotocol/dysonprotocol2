# Ignore everything — only allowlist what Dockerfile.dev COPYs
*

# Files needed by the final stage
!requirements.txt

# Files needed by the dysvm-builder stage
!go.mod
!go.sum
!Makefile
!scripts/
!dysvm/
# Exclude stale build artifacts from submodules (self-hosted runners reuse workspaces)
dysvm/python-build-standalone/build/
dysvm/python-build-standalone/dist/
!cosmos-sdk/
!.git/
